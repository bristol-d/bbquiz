<h3 class="doc">Multiple choice question (${question.config['points']} points)</h3>
<div class="stem">
${fmt(question.stem)}
<ol>
%for option in question.options:
    <li>${fmt(option.text)}</li>
%endfor
</ol>
</div>
<p class="solution">
The correct answer is number ${question.index + 1}.
</p>
<div class="note">${question.note}</div>